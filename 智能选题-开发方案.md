# 爆款罗盘 · 「AI 智能选题」页面开发方案（细化版）

> 文档状态：待开发（方案已细化，开工中）
> 负责人视角：产品通 / 前端 + 轻后端
> 本期范围：**只做「AI 智能选题」单页**，其余模块（热点雷达站、仿写二创、全链路生成）本期不实现，导航占位为"建设中"。

---

## 1. 背景与目标（Why）

创作者选题长期"靠拍脑袋"，缺少热度判断与可复制的产出方法。本期把已打磨好的 **AI 智能选题 Prompt**（5 步流水线 + 质量门 + 红线）落成一个**真实可用的页面**：用户输入热点报告 + 参数，页面调用 AI 返回一批带"传播锚点 / 爆款基因 / 三维打分"的结构化选题，直接可读、可排序、可筛选。

**业务目标**：把"出选题"从 1–2 小时/选题 压缩到分钟级，且用质量门保证下限。
**成功指标（北极星）**：人均单次会话生成可用选题数 ≥ 5，且总分 ≥10 占比 ≥ 80%。

---

## 2. 范围（Scope）

### In（本期做）
- 智能选题页面：参数面板（热点报告输入 / 内容类型多选 / 目标受众多选 / 数量）
- 调用后端 `/api/generate-topics`，渲染选题卡片
- 选题卡片字段：topic / title / angle / anchor / anchor_reason / genes / hit_reason / score
- 结果按 `score.total` 降序；总分 < 10 标注"边缘·建议优化"
- 加载态、错误态、空态
- AI 层**可插拔**：无 API key 时走 mock，填 key 即切真模型（OpenAI 兼容协议）

### Non-goals（本期不做）
- 热点雷达站 / 关键词探索 / 对标监控（数据抓取与监控）
- 爆款仿写与二创（下一个模块）
- 全链路内容生成（标题/正文/配图批量产出，已确认本期不做）
- 用户系统、选题库持久化、付费

---

## 3. 用户故事

- 作为**内容创作者**，我希望粘贴一份热点报告并勾选目标人群/类型，以便快速拿到一批结构化选题，不用自己想。
- 作为**新媒体运营**，我希望选题带打分和基因标签，以便快速判断哪条更该做、便于团队评审。
- 作为**团队负责人**，我希望弱选题被自动标注"边缘"，以便把精力集中在高潜力选题上。

---

## 4. 信息架构 & 页面结构

```
App Shell（深色侧边栏 + 浅色内容区）
├─ 智能选题（本期主页面，active）
├─ 热点雷达站（建设中占位）
├─ 爆款仿写二创（建设中占位）
└─ 设置（建设中占位）

智能选题页布局（桌面端两栏）：
├─ 左栏 320px：参数面板
│   ├─ 热点/趋势报告（多行文本框，必填）
│   ├─ 内容类型（多选 chips：干货教程/测评对比/种草推荐/避坑盘点/vlog/知识科普）
│   ├─ 目标受众（多选 chips：学生党/油痘肌/通勤族/新手妈妈/租房党/健身小白）
│   ├─ 生成数量（3 / 5 / 8 分段）
│   └─ 「✨ 生成选题」按钮（主操作）
└─ 右栏：结果区
    ├─ 加载态（骨架/转圈）
    ├─ 错误态（重试）
    └─ 选题卡片网格（2 列）
        └─ 卡片：标题 → topic → 角度 → 锚点+锚点理由 → 基因 chips → hit_reason → 打分环(click/spread/exec/total) + 边缘标
```

---

## 5. 前后端契约（API）

**POST `/api/generate-topics`**

Request:
```json
{
  "hotspot_summary": "（文本）热门关键词、高频话题、用户痛点、竞品内容缺口",
  "content_types": ["干货教程", "测评对比"],
  "audiences": ["学生党", "油痘肌"],
  "count": 5
}
```

Response 200:
```json
{
  "ok": true,
  "model": "mock | gpt-4o-mini | deepseek-chat …",
  "topics": [
    {
      "topic": "一句话选题（15-25字）",
      "title": "可直接发布的小红书标题（≤20字）",
      "angle": "切入口 + 为何吸引目标受众",
      "anchor": "传播锚点描述（50字内）",
      "anchor_reason": "为何引发截图/收藏",
      "genes": ["情绪钩子", "身份标签"],
      "hit_reason": "与热点报告的关联点",
      "score": { "click": 5, "spread": 4, "exec": 4, "total": 13 }
    }
  ]
}
```

Error:
```json
{ "ok": false, "error": "缺少 hotspot_summary" | "AI 调用失败：…" }
```

**契约约定**
- 后端对 `topics` 按 `score.total` 降序排列后再返回，前端不再排序。
- 真模型调用失败时，后端降级返回 mock 并在 `model` 字段标注，前端提示"已用示例数据"。
- 字段缺失（如模型漏返 `score`）后端补默认值，保证前端不崩。

---

## 6. 关键实现点

1. **Prompt 注入（src/prompt.js）**：把精炼版 Prompt（角色 + 5 步流水线 + 输出格式 + 红线）作为 system，变量拼成 user。热点报告原文透传，不篡改。
2. **质量门**：Prompt 内要求模型自评 click/spread/exec 三维（各 1-5，满分 15），<10 标 edge；后端二次校验并排序。
3. **红线兜底**：Prompt 内置违禁词/不站队/医疗弱化约束；后端对返回文本做违禁词粗筛（最/第一/绝对/国家级），命中则标记 `risk:true` 供前端提示。
4. **可插拔 AI（src/ai.js）**：`VC_LLM_API_KEY` 未设 → mock；设了 → 走 OpenAI 兼容 `/chat/completions`（base/model 可配），解析返回 JSON 数组（容错提取第一个 `[...]`）。
5. **前端只读渲染**：结果区不信任模型，做字段兜底（缺啥补"—"），基因/分数异常不崩。

---

## 7. 文件结构

```
viral-compass/
├─ package.json            # 零依赖，scripts.start = node server.js
├─ server.js               # 纯 node http：静态托管 + /api/generate-topics
├─ src/
│  ├─ prompt.js            # buildPrompt(payload) → {system, user}
│  └─ ai.js                # generateTopics(payload)：mock | 真实LLM
├─ public/
│  ├─ index.html           # App Shell + 智能选题页
│  ├─ styles.css           # 设计 token + 组件样式（沿用原型视觉）
│  └─ app.js               # 参数交互 + 调接口 + 渲染卡片
└─ 智能选题-开发方案.md     # 本文档
```

技术栈：**Node 22（纯 http，零依赖）** + 原生前端（HTML/CSS/JS），与现有数见项目栈一致，部署简单。

---

## 8. 里程碑（建议）

- **M1 骨架**：package.json + server.js + 静态托管 + 空页面壳 ✅（本次开工即做）
- **M2 前端**：参数面板 + 结果卡片 + 加载/错误/空态
- **M3 后端 AI**：prompt.js + ai.js（mock 优先）+ API 联通
- **M4 打磨**：质量门排序、红线标、响应式、埋点（生成数/可用率）

---

## 9. 验收标准（Definition of Done）

- [ ] 填热点报告 + 勾参数，点生成，3s 内出卡片（mock 环境）
- [ ] 卡片完整展示 8 个字段，分数环正确，<10 分标"边缘"
- [ ] 缺热点报告时前端拦截并提示，不发请求
- [ ] 后端 API 返回结构符合契约，topics 已按 total 降序
- [ ] 无 key 走 mock、填 key 走真模型，切换无需改代码
- [ ] 真模型失败自动降级 mock 并提示
- [ ] 响应式：≥1280 两栏，≤768 单栏堆叠

---

## 10. 风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| 真模型返回非标准 JSON | 前端解析失败 | 后端容错提取 + 字段兜底 + 降级 mock |
| 模型忽略红线输出违禁词 | 限流/违规 | Prompt 强约束 + 后端粗筛标记 risk |
| 热点报告过长超 token | 截断/贵 | 前端限制 2000 字，超出提示 |
| 本期只做单页，用户期待整站 | 落差 | 导航明确"建设中"占位，管理预期 |
